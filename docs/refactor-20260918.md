> 本文记录第一次代码拆分；之后的根目录收拢已完成，当前路径请看 [目录整理说明](目录整理说明.md)。

# 2026-09-18 项目目录重构

用户已要求按前端、后端、Python 视觉等职责整理项目，本次直接实施代码目录重构，不涉及正式 Office 交付物。

## 来源与判断

- `raw/README.md`：没有额外原始需求文件，目录保持原样。
- `frontend/package.json`、`frontend/vite.config.ts`、`frontend/dev/camera-plugin.mjs`：Vue 前端通过开发服务器适配器调用根目录 Python 脚本。
- `backend/src/main/java/com/classroom/ai/service/impl/FaceServiceImpl.java`：后端桌面注册入口同样引用根目录 Python。
- 根目录五个视觉 Python 文件是当前运行版本；`services/edge-perception/` 是另一份实现，先完整归档，不混合覆盖。
- `models/` 为当前根目录视觉脚本使用的模型和本地特征库；`uploads/` 为 Java 与视觉模块共享图片路径。
- `tests/` 混合了无硬件单元测试、硬件实验和跨服务测试。
- `docs/`、`memory-bank/project-context.md` 提供历史背景；当前代码是路径调整依据。

## 实施结构

- `frontend/`：Vue 页面、组件、API、前端测试及本地摄像头调用适配器。
- `backend/`：Spring Boot 服务和 Java 测试。
- `vision/`：唯一在用的 Python 视觉实现、模型、依赖、单元测试和硬件诊断。
- `scripts/`：实际启动脚本和统一测试入口；根目录原有启动命令保留为薄转发入口。
- `docker/`：原数据库初始化配置；根目录 Compose 保持有效。
- `tests/`：跨服务 API 和浏览器联调测试。
- `uploads/`：共享运行数据，保持已有 URL 和数据位置。
- `backup/structure_20260918/`：旧服务副本、样例入口、被修改文件的原始版本。

## 验证计划

1. 对移动的模型、原始资料和共享图片核对 SHA-256，确保文件不丢失、不改变。
2. Python 无硬件回归及新目录入口检查。
3. 前端单元测试、类型检查和构建。
4. 后端 Maven 测试及编译。
5. PowerShell 语法检查和路径扫描。

## 验证边界

本次不自动打开摄像头、不新增或覆盖真实人脸记录；摄像头与 GPU 完整识别需现场复核。生产部署中摄像头接口仍依赖原有 Vite 开发适配器，本次为文件职责整理，没有新建独立推理 API 服务。

## 实际完成与验证结果

- 五个根目录视觉脚本及模型迁入 `vision/`；旧 `services/edge-perception/` 完整迁入 `backup/structure_20260918/edge-perception/`，PyCharm 示例 `main.py` 同步归档。
- 原 Python 回归测试迁入 `vision/tests/`；六个手动实验迁入 `vision/diagnostics/`，相对模型路径仍指向同级 `vision/models/`。
- 前端摄像头适配器与 Java 注册入口均改用 `vision/` 下脚本。Java 入口改为直接传递进程参数，避免含空格项目路径经过 `cmd start` 再解释。
- 启动脚本实现在 `scripts/`；原根目录 PowerShell、BAT 命令作为转发入口保留。
- 新增 `vision/paths.py`、包命令入口、核心/可选依赖清单、目录说明和 `.gitignore`。没有重装或搬动现有虚拟环境。
- `scripts/run-tests.ps1 -Offline` 通过：Java **33** 项、前端 **10** 项、Python **5** 项测试；前端 TypeScript 检查及 Vite 构建通过。
- 新增前端回归用无害子进程替代摄像头，验证带空格目录下调用 `vision/face_verify.py`、读取共享上传临时图片并完成清理。
- 新增 Python 回归验证更换工作目录后资源路径稳定，包帮助命令不加载设备依赖。
- `docs/refactor-20260918-files.json` 中 **20** 个文件 SHA-256 核对一致，包括 raw 原始资料、当前模型、共享图片和旧服务归档。
- **7** 个 PowerShell 文件语法通过；**16** 个 Python 文件 AST 语法检查通过。
- 现有 `.venv1` 下，包入口 `register --help`、直接入口 `face_verify.py --help`、`classroom_monitor.py --help` 均退出成功，无需启动摄像头。

## 保留事项

- Vite 构建仍提示单个打包块超过 500 kB；本次没有调整页面加载策略。
- 现有 Python 环境未发现 MediaPipe / Ultralytics，相关实验的完整执行未验证；可选依赖入口已提供。
- 新依赖清单未在全新虚拟环境中验证安装；当前已配置环境未被更改。
- 没有重启用户已运行的服务，也未执行真实人脸录入、联网下载模型或开启摄像头。现有进程需重启后使用新源码。
- 本次交付为直接更新项目源码与运行说明，因此没有在 `output/` 额外生成文档副本。

